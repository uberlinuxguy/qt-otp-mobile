<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the real palette; this theme only shapes the window
         before the first frame, so it stays minimal and needs no AppCompat. -->
    <style name="Theme.QtOtp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
